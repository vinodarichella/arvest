# arvest

Spring boot application connects to Robinhood APIs and provides portfolio report. Uses monogDb as db and Thymeleaf and Bootstrap UI for reporting.

# How to run


```
mvn spring-boot:run
```
